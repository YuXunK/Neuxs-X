<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yuxun.x.nexusx.Mapper.UserMapper">
    <!-- 检查用户名是否存在 -->
    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM users
        WHERE username = #{username}
    </select>

    <!-- 检查手机号码是否存在 -->
    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM users
        WHERE phone = #{phone}
    </select>

    <!-- 检查邮箱地址是否存在 -->
    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 保存用户信息 -->
    <insert id="insertUser">
        INSERT INTO users (username, phone, email, password, status, createdAt, updatedAt)
        VALUES (#{username}, #{phone}, #{email}, #{password}, #{status}, #{createdAt}, #{updatedAt})
    </insert>
</mapper>
